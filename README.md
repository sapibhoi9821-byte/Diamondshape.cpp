# Diamondshape.cpp
A C++ console program that prints a diamond shape of asterisks (*). The user inputs any positive integer to set the size, and the program generates the corresponding diamond using nested loops.
